# ============================================================
# Sentiment Analysis REST API — Production Dependencies
# Python 3.10+
# ============================================================

# ----------------------------
# Deep Learning & Model Training
# ----------------------------
torch==2.2.1
torchvision==0.17.1                  # Required for some torch utilities
transformers==4.39.3                 # HuggingFace BERT / DistilBERT
tokenizers==0.15.2                   # Fast HuggingFace tokenizers
datasets==2.18.0                     # HuggingFace datasets utility
accelerate==0.28.0                   # Mixed precision + distributed training
onnx==1.16.0                         # ONNX model export
onnxruntime==1.17.3                  # ONNX CPU inference (faster than PyTorch on CPU)

# ----------------------------
# Data Processing & NLP
# ----------------------------
pandas==2.2.1
numpy==1.26.4
nltk==3.8.1
spacy==3.7.4
langdetect==1.0.9                    # Language detection and filtering
contractions==0.1.73                 # Expand contractions (don't → do not)
emoji==2.11.0                        # Emoji handling / conversion
beautifulsoup4==4.12.3               # HTML entity decoding
pyarrow==15.0.2                      # Parquet file read/write
fastparquet==2024.2.0                # Alternative Parquet engine

# ----------------------------
# API Framework
# ----------------------------
fastapi==0.110.1
uvicorn[standard]==0.29.0            # ASGI server with websocket + reload support
pydantic==2.6.4                      # Request/response schema validation
pydantic-settings==2.2.1             # Environment variable management
python-multipart==0.0.9              # Form data / file upload support
httpx==0.27.0                        # Async HTTP client (used in tests + health checks)

# ----------------------------
# Rate Limiting & Middleware
# ----------------------------
slowapi==0.1.9                       # Rate limiting (Limits by IP)
starlette==0.37.2                    # ASGI toolkit (FastAPI dependency)

# ----------------------------
# Database
# ----------------------------
asyncpg==0.29.0                      # Async PostgreSQL driver
sqlalchemy==2.0.29                   # ORM for prediction logging
alembic==1.13.1                      # Database migrations

# ----------------------------
# Caching
# ----------------------------
redis==5.0.3                         # Redis client for prediction caching
hiredis==2.3.2                       # High-performance Redis parser

# ----------------------------
# Monitoring & Metrics
# ----------------------------
prometheus-fastapi-instrumentator==6.1.0   # Auto-expose /metrics for FastAPI
prometheus-client==0.20.0                  # Custom counters and histograms

# ----------------------------
# Logging
# ----------------------------
structlog==24.1.0                    # JSON-formatted structured logging

# ----------------------------
# Environment & Config
# ----------------------------
python-dotenv==1.0.1                 # Load .env variables at runtime
pyyaml==6.0.1                        # Parse config.yaml

# ----------------------------
# Experiment Tracking
# ----------------------------
mlflow==2.11.3                       # Experiment logging and model registry

# ----------------------------
# Utilities
# ----------------------------
scikit-learn==1.4.1.post1            # Metrics: F1, confusion matrix, stratified split
scipy==1.13.0                        # Supporting math utilities
tqdm==4.66.2                         # Progress bars during training/preprocessing